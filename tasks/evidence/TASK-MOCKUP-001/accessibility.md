# Keyboard and accessible names — TASK-MOCKUP-001

Status: PASS

Nine consecutive keyboard Tab presses reached, in order: Back to portfolio, Reset, Export PNG, Replace image, Background, Padding, Scale, Horizontal, and Vertical.

For every stop, Chrome computed `outline-style: solid`, `outline-width: 2px`, and white outline color. Every control has a visible text label or accessible name. The icon-only back action exposes `Back to portfolio`; decorative Phosphor icons are hidden from the accessibility tree.

Raw evidence: `browser-report.json.focus` and `browser-report.json.valid.labels`.
